import type { NextConfig } from "next";

// 正式環境：在 Vercel → Project → Settings → Environment Variables 設定
// NEXT_PUBLIC_*、SUPABASE_SERVICE_ROLE_KEY、AUTH_*、ADMIN_EMAIL_ALLOWLIST（勿依賴 .env.local）

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
