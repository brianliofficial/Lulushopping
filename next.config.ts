import type { NextConfig } from "next";

// 正式環境：在 Vercel → Project → Settings → Environment Variables 設定
// NEXT_PUBLIC_* 與 SUPABASE_SERVICE_ROLE_KEY、PRODUCTS_ADMIN_SECRET（勿依賴 .env.local）

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
